# 📐 Mimari Diyagramlar ve Görsel Açıklamalar

Bu doküman, İşletme Randevu Sistemi projesinin mimari yapısını görsel olarak açıklar.

## 📊 Genel Mimari Diyagramı

```
┌─────────────────────────────────────────────────────────────────────┐
│                         ANGULAR APPLICATION                          │
├─────────────────────────────────────────────────────────────────────┤
│                                                                       │
│  ┌──────────────────┐  ┌──────────────────┐  ┌──────────────────┐  │
│  │   Components     │  │   Components     │  │   Components     │  │
│  │   (Anasayfa)     │  │     (Login)      │  │(İşletme Panel)   │  │
│  └────────┬─────────┘  └────────┬─────────┘  └────────┬─────────┘  │
│           │                     │                     │             │
│           └─────────────────────┼─────────────────────┘             │
│                                 │                                   │
│                    ┌────────────▼──────────────┐                    │
│                    │    Services Layer         │                    │
│                    │  ┌────────────────────┐   │                    │
│                    │  │   AuthService      │   │                    │
│                    │  │  (BehaviorSubject) │   │                    │
│                    │  └────────────────────┘   │                    │
│                    │  ┌────────────────────┐   │                    │
│                    │  │  IsletmeService    │   │                    │
│                    │  └────────────────────┘   │                    │
│                    └────────────┬──────────────┘                    │
│                                 │                                   │
│                    ┌────────────▼──────────────┐                    │
│                    │   HTTP Interceptors       │                    │
│                    │  ┌────────────────────┐   │                    │
│                    │  │  AuthInterceptor   │   │                    │
│                    │  │  (Add JWT Token)   │   │                    │
│                    │  └────────────────────┘   │                    │
│                    │  ┌────────────────────┐   │                    │
│                    │  │  ErrorInterceptor  │   │                    │
│                    │  │  (Error Handling)  │   │                    │
│                    │  └────────────────────┘   │                    │
│                    └────────────┬──────────────┘                    │
│                                 │                                   │
└─────────────────────────────────┼───────────────────────────────────┘
                                  │
                     ┌────────────▼──────────────┐
                     │      HTTP Client          │
                     │   (Angular HttpClient)    │
                     └────────────┬──────────────┘
                                  │
                     ┌────────────▼──────────────┐
                     │     Backend API           │
                     │  (Laravel REST API)       │
                     └───────────────────────────┘
```

## 🔄 State Management Flow

### 1. Authentication State Flow

```
┌──────────────────────────────────────────────────────────────────┐
│                      LOGIN FLOW                                   │
└──────────────────────────────────────────────────────────────────┘

User Input (LoginComponent)
    │
    │ onLogin()
    ▼
┌─────────────────────┐
│  LoginComponent     │
│  loginTelefon       │──────┐
│  loginPassword      │      │
│  loading = true     │      │ authService.login()
└─────────────────────┘      │
                             ▼
                    ┌─────────────────────┐
                    │   AuthService       │
                    │  login(tel, pass)   │
                    └──────────┬──────────┘
                               │
                               │ HTTP POST /login
                               ▼
                    ┌─────────────────────┐
                    │   Backend API       │
                    │  Return: {token,    │
                    │          isletme}   │
                    └──────────┬──────────┘
                               │
                               │ .pipe(tap())
                               ▼
                    ┌─────────────────────┐
                    │  Side Effects       │
                    │  1. setToken()      │────► localStorage.setItem('token')
                    │  2. setStoredIsletme() ──► localStorage.setItem('isletme')
                    │  3. isLoggedIn.next()  ──► BehaviorSubject.next(true)
                    └──────────┬──────────┘
                               │
                               │ subscribe()
                               ▼
                    ┌─────────────────────┐
                    │  LoginComponent     │
                    │  loading = false    │
                    │  router.navigate()  │
                    └─────────────────────┘
```

### 2. Data Loading State Flow

```
┌──────────────────────────────────────────────────────────────────┐
│                   DATA LOADING FLOW                               │
└──────────────────────────────────────────────────────────────────┘

Component Initialization
    │
    │ ngOnInit()
    ▼
┌─────────────────────┐
│  AnasayfaComponent  │
│  yukleniyor = true  │
│  isletmeler = []    │
└──────────┬──────────┘
           │
           │ isletmeService.getIsletmeler()
           ▼
┌─────────────────────┐
│  IsletmeService     │
│  HTTP GET /isletme  │
└──────────┬──────────┘
           │
           │ Observable<Isletme[]>
           ▼
┌─────────────────────┐
│  subscribe({})      │
│    next: (data) =>  │──────► isletmeler = data
│                     │──────► yukleniyor = false
│    error: (err) =>  │──────► console.error()
│                     │──────► yukleniyor = false
└─────────────────────┘
           │
           ▼
      [Template Render]
         *ngIf="!yukleniyor"
         *ngFor="let item of isletmeler"
```

## 🎯 Component State Lifecycle

```
┌────────────────────────────────────────────────────────────────┐
│              COMPONENT LIFECYCLE & STATE                        │
└────────────────────────────────────────────────────────────────┘

1. INITIALIZATION
   ┌─────────────────┐
   │  constructor()  │──► Dependency Injection
   └────────┬────────┘
            │
            ▼
   ┌─────────────────┐
   │   ngOnInit()    │──► Load initial data
   │                 │──► Subscribe to observables
   │                 │──► Initialize state
   └────────┬────────┘
            │
            ▼
2. RUNTIME
   ┌─────────────────┐
   │  User Actions   │──► onClick(), onChange()
   │                 │──► Update local state
   │                 │──► Call services
   └────────┬────────┘
            │
            ▼
   ┌─────────────────┐
   │  State Changes  │──► Component re-renders
   │                 │──► Template updates
   └────────┬────────┘
            │
            ▼
3. CLEANUP
   ┌─────────────────┐
   │  ngOnDestroy()  │──► Unsubscribe
   │                 │──► Clear state
   │                 │──► Cleanup resources
   └─────────────────┘
```

## 📦 Service State Pattern

```
┌────────────────────────────────────────────────────────────────┐
│            SERVICE STATE WITH BEHAVIORSUBJECT                   │
└────────────────────────────────────────────────────────────────┘

AuthService
├── Private State (BehaviorSubject)
│   ├── isLoggedInSubject: BehaviorSubject<boolean>
│   └── currentIsletmeSubject: BehaviorSubject<Isletme | null>
│
├── Public Observables (Read-only)
│   ├── isLoggedIn$: Observable<boolean>
│   └── currentIsletme$: Observable<Isletme | null>
│
├── State Getters (Synchronous)
│   ├── isLoggedIn(): boolean
│   └── getCurrentIsletme(): Isletme | null
│
└── State Setters (Private)
    ├── setToken(token: string): void
    │      ├── localStorage.setItem('token', token)
    │      └── isLoggedInSubject.next(true)
    │
    └── removeToken(): void
           ├── localStorage.removeItem('token')
           └── isLoggedInSubject.next(false)


┌─────────────────────────────────────────────────────────────┐
│  Components Subscribe to State Changes                      │
└─────────────────────────────────────────────────────────────┘

Component A                Component B                Component C
    │                          │                          │
    │ subscribe()              │ subscribe()              │ subscribe()
    ▼                          ▼                          ▼
┌───────────────────────────────────────────────────────────────┐
│              authService.isLoggedIn$ (Observable)             │
└───────────────────────────────────────────────────────────────┘
                               │
                               │ BehaviorSubject
                               ▼
                        ┌──────────────┐
                        │  State: true │ ◄── All components get updated
                        └──────────────┘
```

## 🔐 HTTP Interceptor Flow

```
┌────────────────────────────────────────────────────────────────┐
│                  HTTP INTERCEPTOR CHAIN                         │
└────────────────────────────────────────────────────────────────┘

Component/Service calls HTTP
         │
         │ http.get/post/patch/delete
         ▼
┌─────────────────────┐
│  HttpClient         │
└──────────┬──────────┘
           │
           │ HTTP Request
           ▼
┌─────────────────────────────────┐
│   AuthInterceptor               │
│  ┌───────────────────────────┐  │
│  │ 1. Get token from storage │  │
│  │ 2. Clone request          │  │
│  │ 3. Add Authorization      │  │
│  │    header: Bearer {token} │  │
│  └───────────────────────────┘  │
└──────────┬──────────────────────┘
           │
           │ Modified Request (with token)
           ▼
┌─────────────────────┐
│   Backend API       │
│   (Laravel)         │
└──────────┬──────────┘
           │
           │ HTTP Response / Error
           ▼
┌─────────────────────────────────┐
│   ErrorInterceptor              │
│  ┌───────────────────────────┐  │
│  │ catchError()              │  │
│  │ 1. Extract error message  │  │
│  │ 2. Log error              │  │
│  │ 3. Normalize error format │  │
│  │ 4. throwError()           │  │
│  └───────────────────────────┘  │
└──────────┬──────────────────────┘
           │
           │ Normalized Response/Error
           ▼
┌─────────────────────┐
│  Component/Service  │
│  subscribe({        │
│    next: ...,       │
│    error: ...       │
│  })                 │
└─────────────────────┘
```

## 💾 LocalStorage Persistence

```
┌────────────────────────────────────────────────────────────────┐
│              LOCALSTORAGE STATE PERSISTENCE                     │
└────────────────────────────────────────────────────────────────┘

┌─────────────────────┐
│   User Login        │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────────────────────┐
│  AuthService.login()                │
│  Response: { token, isletme }       │
└──────────┬──────────────────────────┘
           │
           ▼
┌─────────────────────────────────────┐
│  Save to LocalStorage               │
│  ┌───────────────────────────────┐  │
│  │ localStorage.setItem(         │  │
│  │   'token',                    │  │
│  │   response.token              │  │
│  │ )                             │  │
│  └───────────────────────────────┘  │
│  ┌───────────────────────────────┐  │
│  │ localStorage.setItem(         │  │
│  │   'isletme',                  │  │
│  │   JSON.stringify(isletme)     │  │
│  │ )                             │  │
│  └───────────────────────────────┘  │
└──────────┬──────────────────────────┘
           │
           │ (Browser Refresh / Close)
           │
           ▼
┌─────────────────────────────────────┐
│  Page Reload                        │
└──────────┬──────────────────────────┘
           │
           ▼
┌─────────────────────────────────────┐
│  AuthService Constructor            │
│  ┌───────────────────────────────┐  │
│  │ token = localStorage.getItem( │  │
│  │   'token'                     │  │
│  │ )                             │  │
│  │                               │  │
│  │ isLoggedInSubject =           │  │
│  │   new BehaviorSubject(        │  │
│  │     !!token                   │  │ ◄── State restored!
│  │   )                           │  │
│  └───────────────────────────────┘  │
└─────────────────────────────────────┘
```

## 📱 Component Communication

```
┌────────────────────────────────────────────────────────────────┐
│           COMPONENT COMMUNICATION PATTERNS                      │
└────────────────────────────────────────────────────────────────┘

1. Parent ──► Child (Input Binding)
   ┌─────────────────┐
   │  ParentComp     │
   │  data = [...]   │
   └────────┬────────┘
            │
            │ [data]="data"
            ▼
   ┌─────────────────┐
   │  ChildComp      │
   │  @Input() data  │
   └─────────────────┘

2. Child ──► Parent (Output Binding)
   ┌─────────────────┐
   │  ChildComp      │
   │  @Output() event│
   └────────┬────────┘
            │
            │ (event)="handler($event)"
            ▼
   ┌─────────────────┐
   │  ParentComp     │
   │  handler(data)  │
   └─────────────────┘

3. Sibling ◄──► Sibling (via Service)
   ┌─────────────────┐        ┌─────────────────┐
   │  ComponentA     │        │  ComponentB     │
   │  publish()      │        │  subscribe()    │
   └────────┬────────┘        └────────▲────────┘
            │                          │
            └───►┌──────────────┐◄─────┘
                 │ SharedService │
                 │ BehaviorSubject
                 └──────────────┘

4. Route Data (via Router)
   ┌─────────────────┐
   │  ComponentA     │
   │  router.navigate│
   │  (['/route',id])│
   └────────┬────────┘
            │
            │ route params
            ▼
   ┌─────────────────┐
   │  ComponentB     │
   │  route.params   │
   │  .subscribe()   │
   └─────────────────┘
```

## 🎨 State Update Patterns

```
┌────────────────────────────────────────────────────────────────┐
│                  STATE UPDATE PATTERNS                          │
└────────────────────────────────────────────────────────────────┘

1. SYNCHRONOUS UPDATE (Local State)
   ┌──────────────────┐
   │  User Action     │──► toggleDarkMode()
   └────────┬─────────┘
            │
            ▼
   ┌──────────────────┐
   │  Update State    │──► this.isDarkMode = !this.isDarkMode
   └────────┬─────────┘
            │
            ▼
   ┌──────────────────┐
   │  Side Effect     │──► localStorage.setItem('darkMode', ...)
   └────────┬─────────┘
            │
            ▼
      [UI Re-renders]


2. ASYNCHRONOUS UPDATE (API Call)
   ┌──────────────────┐
   │  User Action     │──► randevuAl()
   └────────┬─────────┘
            │
            ▼
   ┌──────────────────┐
   │  Loading State   │──► this.loading = true
   └────────┬─────────┘
            │
            ▼
   ┌──────────────────┐
   │  API Call        │──► service.randevuAl().subscribe()
   └────────┬─────────┘
            │
            ├──► Success ──► Update State ──► loading = false
            │
            └──► Error ──► Show Error ──► loading = false


3. OPTIMISTIC UPDATE
   ┌──────────────────┐
   │  User Action     │──► randevuOnayla()
   └────────┬─────────┘
            │
            ▼
   ┌──────────────────┐
   │  Update UI First │──► randevu.durum = 'onaylandi'
   └────────┬─────────┘
            │
            ▼
   ┌──────────────────┐
   │  API Call        │──► service.randevuOnayla()
   └────────┬─────────┘
            │
            ├──► Success ──► Keep UI
            │
            └──► Error ──► Rollback UI
```

## 📊 State Hierarchy

```
┌────────────────────────────────────────────────────────────────┐
│                     STATE HIERARCHY                             │
└────────────────────────────────────────────────────────────────┘

Application
├── Global State (Services)
│   ├── AuthService
│   │   ├── isLoggedIn: boolean
│   │   ├── currentIsletme: Isletme | null
│   │   └── token: string | null
│   │
│   └── ThemeService (Hypothetical)
│       └── isDarkMode: boolean
│
├── Route-Level State
│   ├── AnasayfaComponent
│   │   ├── isletmeler: Isletme[]
│   │   ├── filtrelenmisIsletmeler: Isletme[]
│   │   └── mevcutSayfa: number
│   │
│   ├── LoginComponent
│   │   ├── loginTelefon: string
│   │   ├── loginPassword: string
│   │   └── loading: boolean
│   │
│   └── IsletmePanelComponent
│       ├── randevular: Randevu[]
│       └── calisanlar: Calisan[]
│
└── Component-Level State
    ├── Modal State
    │   ├── modalAcik: boolean
    │   └── seciliIsletme: Isletme | null
    │
    └── Form State
        ├── formValues: any
        └── validation: ValidationState
```

---

**Not**: Bu diyagramlar ASCII art formatındadır. Daha gelişmiş görselleştirme için PlantUML, Mermaid.js veya draw.io gibi araçlar kullanılabilir.

**Son Güncelleme**: 2 Ocak 2026
